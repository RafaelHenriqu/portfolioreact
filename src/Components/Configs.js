/*
   
    let Perfil = document.getElementById('Perfil').style

    Perfil.boxShadow = `0px 0px 50px  hsla(240, 100%, 50%, 0.${String(Hora).substring(1)})`
   
    if (Hora > 19 || Hora < 11)
    {
     Perfil.boxShadow = `0px 0px 50px  hsla(240, 100%, 50%, 0.9)`
     
    }
     
*/
function Configs({Hora})
{
 

}

export default Configs 